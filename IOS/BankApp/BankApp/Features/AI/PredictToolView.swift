//
//  PredictToolView.swift
//  BankApp
//
//  Created by Вадим Семибратов on 04.06.2025.
//

import SwiftUI

struct PredictToolView: View {
    var body: some View {
        ScrollView {
            VStack(alignment: .leading, spacing: 16) {
                Text("Инструменты анализа рынка")
                    .font(.title)
                    .fontWeight(.bold)
                    .padding(.top)
                
                Group {
                    Text("📊 Фундаментальный анализ")
                        .font(.headline)
                    Text("""
Фундаментальный анализ основан на изучении реальных экономических факторов: отчётности компаний, решений центральных банков, геополитических событий. Цель — определить, насколько актив переоценён или недооценён.

Например, рост процентных ставок ФРС США может привести к удорожанию доллара, так как это делает долларовые активы более привлекательными.
""")
                    
                    Text("📈 Технический анализ")
                        .font(.headline)
                    Text("""
Технический анализ использует графики цен и объёмов торгов для прогнозирования движения рынка. Основные инструменты: трендовые линии, уровни поддержки и сопротивления, свечные модели, индикаторы (RSI, MACD, Bollinger Bands).

Например, если цена актива дважды отскакивает от одной и той же отметки, это может указывать на уровень поддержки.
""")
                }
                
                Group {
                    Text("🐂 Бычий и 🐻 медвежий рынки")
                        .font(.headline)
                    Text("""
**Бычий рынок** (bull market) — фаза роста, оптимизма и уверенности. Пример: рост фондового рынка США в 2010–2021 гг.

**Медвежий рынок** (bear market) — фаза снижения, страха и пессимизма. Пример: резкое падение рынков в марте 2020 года из-за пандемии COVID-19.
""")
                    
                    Text("🟢 Лонг и 🔴 Шорт")
                        .font(.headline)
                    Text("""
**Лонг** (long) — покупка актива в ожидании его роста. Трейдер зарабатывает, если цена увеличивается.

**Шорт** (short) — продажа актива, которого у трейдера нет, с надеждой выкупить его позже дешевле. Используется для заработка на падении. Часто сопровождается повышенными рисками.
""")
                }
                
                Group {
                    Text("🎬 История: \"Игра на понижение\"")
                        .font(.headline)
                    Text("""
Фильм **«Игра на понижение» (The Big Short, 2015)** основан на реальных событиях. Группа инвесторов во главе с Майклом Бьюрри предсказала ипотечный кризис 2007–2008 годов и «зашортила» ипотечные облигации.

Их стратегия принесла миллиарды долларов прибыли, но в то время Wall Street считала их безумцами. Это отличный пример того, как глубокий анализ может выявить угрозы, игнорируемые большинством.
""")
                    
                    Text("🧠 Заключение")
                        .font(.headline)
                    Text("""
Анализ рынка требует сочетания логики, наблюдательности и терпения. Используйте как фундаментальный, так и технический анализ для формирования собственной точки зрения. Изучайте истории успеха и ошибок, чтобы избегать эмоциональных решений и делать ставки на основе фактов.
""")
                    
                    Text("🕯 Свечной анализ и японские свечи")
                        .font(.title2)
                        .bold()
                    
                    Text("""
Свечной анализ — популярный инструмент технического анализа. Он позволяет трейдерам видеть не просто цену, а поведение участников рынка в течение каждого периода.

Каждая **японская свеча** отражает цену открытия, закрытия, максимум и минимум. Цвет и форма свечи дают подсказки о рыночных настроениях.

Существуют типичные свечные модели:
• **Молот** — сигнал возможного разворота вверх  
• **Поглощение** — сильный сигнал на разворот  
• **Доджи** — неопределённость на рынке  
• **Три белых солдата** — продолжение роста  
• **Три чёрные вороны** — признак падения

Ниже — пример японских свечей с пояснением моделей:
""")
                    
                    // Центрированное изображение
                    HStack {
                        Spacer()
                        Image("formacii") // Замени на свою картинку
                            .resizable()
                            .scaledToFit()
                            .frame(height: 200)
                            .cornerRadius(12)
                            .overlay(Text("Здесь будет изображение японских свечей")
                                .foregroundColor(.white)
                                .padding(.horizontal, 8), alignment: .center)
                        Spacer()
                    }
                }
            }
            .padding()
            .foregroundColor(.white)
        }
        .background(
            LinearGradient(
                gradient: Gradient(colors: [
                    Color(red: 0.06, green: 0.04, blue: 0.15),
                    Color(red: 0.15, green: 0.08, blue: 0.35),
                    Color(red: 0.25, green: 0.13, blue: 0.45)
                ]),
                startPoint: .top,
                endPoint: .bottom
            )
            .ignoresSafeArea()
        )
    }
}
